/**
 * main.js — Entry point
 * Modules are loaded via individual <script> tags in HTML.
 * This file handles any cross-module initialisation if needed.
 */

// Nothing required here yet — each module is self-initialising.
// Add global helpers or shared state here as the project grows.
